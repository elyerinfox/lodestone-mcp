{
  "name": "lodestone-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Nuxt 3 dashboard for lodestone-mcp — connects to /ws/status for live server, memory, and constellation snapshots.",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.0",
    "@types/node": "^22.0.0",
    "nuxt": "^3.13.0",
    "typescript": "^5.5.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "dependencies": {
    "@vueuse/core": "^11.0.0",
    "@vueuse/nuxt": "^11.0.0"
  }
}
