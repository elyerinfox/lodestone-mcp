# -----------------------------------------------------------------------------
# Provider: mdn  (kind: docs)
# -----------------------------------------------------------------------------
# MDN Web Docs reference (JavaScript/CSS/HTML/Web APIs). Keyless JSON search of
# developer.mozilla.org/api/v1/search?q=. No tunables. Powers docs_search and the
# per-provider docs_mdn tool.
#
# Enable/disable in ../02-search.toml (the docs list).
# -----------------------------------------------------------------------------
