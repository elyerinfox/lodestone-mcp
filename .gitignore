/target

# JetBrains IDE files
/.idea/

# Personal overrides, layered on top of the committed config/ baseline. May hold
# machine-specific paths or tokens, so it's not committed.
/lodestone.toml

# On-disk file store ([store]) and any test store dirs — runtime data, not source.
# Unrooted so a stray `cargo test` / dev-binary run from a subdir doesn't slip
# runtime DBs back into the tree.
.lodestone-store/
.store-test/

# Memory/solution SQLite store ([memory]) — runtime data, never source.
.lodestone-memory/

# Local smoke/dev harness directory — runtime configs + state, never source.
/smoke/

# Claude Code session state.
/.claude/
